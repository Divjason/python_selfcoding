menu = {"커피", "우유", "주스"}
print(menu)
print(type(menu))

menu = list(menu) #리스트로 변환
print(menu, type(menu))

menu = tuple(menu) #튜플로 변환
print(menu, type(menu))

menu = set(menu) #세트로 변환
print(menu, type(menu))

